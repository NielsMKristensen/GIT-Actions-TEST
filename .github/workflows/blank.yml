demo:
    runs-on: ubuntu-latest
    steps:
     - uses: actions/checkout@v2
     - id: generatePassword
       uses: NielsMKristensen/Password-Generator@v0.1
     - name: show password
       run: echo "${{ steps.generatePassword.outputs.password }}"
       shell: bash 
    on:
        push:
            tags: - v0.1
   
